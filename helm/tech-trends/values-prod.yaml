namespace:
  name: production
 
service:
  port: 80
  type: LoadBalancer

image:
  repository: tech-trends
  tag: web
  pullPolicy: IfNotPresent

replicaCount: 3

resources:
  requests:
    cpu: 60m
    memory: 256Mi
