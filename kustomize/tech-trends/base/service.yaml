apiVersion: v1
kind: Service
metadata:
  name: tech-trends-service
  namespace: test
  labels:
    app: tech-trends
    tag: web
spec:
  selector:
    app: tech-trends
    tag: web
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3111
    nodePort: 5000
  type: NodePort
